databases:
  - name: StringCalculator
    databaseName: StringCalculator
    user: StringCalculator
    plan: free

services:
  - type: web
    plan: free
    name: StringCalculator
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: StringCalculator
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
